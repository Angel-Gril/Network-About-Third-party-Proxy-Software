# 参与开发

先阅读 [项目结构](docs/ARCHITECTURE.md)、[开发与验证](docs/DEVELOPMENT.md) 和 [开发约定](AGENTS.md)。

修改前定位代码所属的提供者或应用，复用现有实现；路径与公共入口变化同步测试、文档和 CI，并在 [迁移说明](docs/MIGRATION.md) 记录用户需要调整的调用方式。

提交前运行受影响测试与 `npm run verify`，检查 diff 和完整暂存内容。使用 `feat:`、`fix:`、`refactor:`、`docs:`、`test:` 或 `ci:` 等清楚的提交前缀。PR 说明具体问题、最终行为和实际验证结果。

问题报告和测试样本使用合成数据。不要提交账号、密码摘要、会话、节点凭据、完整订阅链接或私有服务器记录；真实验收结果只报告脱敏证据和明确的验证范围。
